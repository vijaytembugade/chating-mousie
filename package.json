{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "tsc -b && node --watch dist/app.js set NODE_ENV=local",
    "prod": "tsc -b && node --watch dist/app.js set NODE_ENV=prod"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/ws": "^8.18.1",
    "node-cron": "^4.2.1",
    "ws": "^8.18.3"
  }
}
